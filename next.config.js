/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "jsonplaceholder.typicode.com",
            "images.dog.ceo"
        ]
    }
}

module.exports = nextConfig
